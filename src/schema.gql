# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type PostTag {
  id: ID!
  name: String!
  postId: String!
}

type PostVote {
  id: ID!
  userId: String!
  postId: String!
  voteType: VoteType!
  voteTime: DateTime!
}

enum VoteType {
  LIKE
  DISLIKE
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Post {
  createAt: DateTime!
  updatedAt: DateTime!
  deletedAt: DateTime
  id: ID!
  userId: String!
  title: String!
  description: String!
  postTags: [PostTag!]
  postVotes: [PostVote!]
}

type User {
  id: ID!
  email: String!
  posts: [Post!]
  postVotes: [PostVote!]
}

type PageInfo {
  startCursor: String
  endCursor: String
  hasPreviousPage: Boolean!
  hasNextPage: Boolean!
}

type PaginatedPost {
  edges: [PostEdge!]
  pageInfo: PageInfo
}

type PostEdge {
  cursor: String!
  node: Post!
}

type Query {
  post(id: ID!): PostVote!
  getPosts(first: Int, after: String, last: Int, before: String): PaginatedPost!
  getPostsByLikeCount: [Post!]!
  getFreshPosts: [Post!]!
  getPostsByCreateAt: [Post!]!
  getMostLikedPosts: [Post!]!
  getMostLiked24hPosts: [Post!]!
  getPostsWithTag(tag: String!): [Post!]!
  getPostsWithTags(tags: [String!]!): [Post!]!
  user(id: ID!): User!
  postTag(id: ID!): PostTag!
}
