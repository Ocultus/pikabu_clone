# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type PostVote {
  id: ID!
  userId: String!
  postId: String!
  voteType: VoteType!
  voteTime: DateTime!
}

enum VoteType {
  LIKE
  DISLIKE
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type CommentVote {
  id: ID!
  userId: String!
  commentId: String!
  voteType: VoteType!
  voteTime: DateTime!
}

type Comment {
  id: ID!
  text: String!
  userId: String!
  postId: String!
  commentVotes: [CommentVote!]
}

type PostTag {
  id: ID!
  name: String!
  postId: String!
}

type Post {
  createAt: DateTime!
  updatedAt: DateTime!
  deletedAt: DateTime
  id: ID!
  userId: String!
  title: String!
  description: String!
  postTags: [PostTag!]
  postVotes: [PostVote!]
  comments: [Comment!]
}

type User {
  id: ID!
  email: String!
  posts: [Post!]
  postVotes: [PostVote!]
  comments: [Comment!]
  commentVotes: [CommentVote!]
}

type PageInfo {
  startCursor: String
  endCursor: String
  hasPreviousPage: Boolean!
  hasNextPage: Boolean!
}

type PaginatedPost {
  edges: [PostEdge!]
  pageInfo: PageInfo
}

type PostEdge {
  cursor: String!
  node: Post!
}

type PaginatedComment {
  edges: [CommentEdge!]
  pageInfo: PageInfo
}

type CommentEdge {
  cursor: String!
  node: Comment!
}

type Query {
  post(id: ID!): PostVote!
  getPosts(first: Int, after: String, last: Int, before: String): PaginatedPost!
  getPostsByLikeCount: [Post!]!
  getFreshPosts: [Post!]!
  getPostsByCreateAt: [Post!]!
  getMostLikedPosts: [Post!]!
  getMostLiked24hPosts: [Post!]!
  getPostsWithTag(tag: String!): [Post!]!
  getPostsWithTags(tags: [String!]!): [Post!]!
  getHotPost: [Post!]!
  user(id: ID!): User!
  comment(id: ID!): Comment!
  getComments(first: Int, after: String, last: Int, before: String): PaginatedComment!
  getCommentByLikeCount: [Comment!]!
  getCommentByCreateAt: [Comment!]!
  postTag(id: ID!): PostTag!
}
